import React from 'react';
import {StyleSheet} from 'react-native';

const LinkedListVisualizer = (): JSX.Element => {
  return <></>;
};

const styles = StyleSheet.create({});

export default LinkedListVisualizer;
